function [ M ] = rotY( angle )
    %ROTY Summary of this function goes here
    %   Detailed explanation goes here
    M = eye(4);
    M(1,1) = cos(angle);
    M(3, 3) = cos(angle);
    M(1, 3) = -sin(angle);
    M(3, 1) = sin(angle);
end

